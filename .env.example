# Pocket TTS Environment Variables for Docker Compose
# Copy this file to .env and configure as needed

# Python settings
PYTHONUNBUFFERED=1

# Note: Model weights are automatically downloaded from HuggingFace Hub on first run
# and cached in the /root/.cache/pocket_tts volume.
# You can customize the default voice by modifying the serve command in Dockerfile
# or by passing environment variables if supported in future versions.

# Optional: Override default voice (currently requires modifying Dockerfile CMD)
# DEFAULT_VOICE=alba

# Optional: Override default host/port (currently requires modifying Dockerfile CMD)
# SERVER_HOST=0.0.0.0
# SERVER_PORT=8000
